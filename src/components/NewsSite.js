/* eslint-disable import/default */

import React from 'react';

const NewsSite = (props) => {
    return ( 
        <div>
            <div>{props.name}</div>
        </div>
    );
};

export default NewsSite;